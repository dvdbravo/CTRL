<%= form_with(model: umedida, local: true) do |form| %>
  <% if umedida.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(umedida.errors.count, "error") %> prohibited this umedida from being saved:</h2>

      <ul>
        <% umedida.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
